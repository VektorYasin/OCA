package q005;

public class sWitch {
    /*
    the expression in a switch statement must evaluate to a single value,

    the case label literals in a switch statement are determined at compile-time and
    cannot be changed at runtime.

     the expression in a switch statement must evaluate to a single value,
     not a collection of values.

     The break statement, at the end of each case block, is optional.
     */
}
